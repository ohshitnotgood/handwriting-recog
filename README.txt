To start the server, `cd` into `inference` directory and run `Flask run`. Read more about dependencies in ./inference/README.txt

To start the Svelte server, `cd` into `server` and run `pnpm dev`. Read more about dependencies in ./server/README.txt

NOTE: This is only a demo of CNNs over MNIST dataset and was never meant for any form of production.